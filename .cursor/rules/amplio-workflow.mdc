---
description: Amplio 项目上下文、代码修改、提交与推送规则
alwaysApply: true
---

# Amplio 项目工作规则

## 开始任务

- 处理本项目的开发、审查或上架任务前，先阅读 `docs/AMPLIO_PROJECT_CONTEXT.md`。
- 该文档是产品状态、合规要求和待办事项的主要来源；若文档与代码不一致，先核实代码并向用户说明。
- 保留用户已有且与当前任务无关的改动，不擅自覆盖、清理或扩大任务范围。

## 修改与确认

- 完成修改后，运行与改动风险相称的分析和测试，并向用户简短汇报改动与检查结果。
- 汇报后停止，等待用户审查；用户点击 **Keep** 只表示保留文件改动，不代表授权提交。
- 用户明确回复“确认提交”前，不得执行 `git add`、`git commit` 或修改已有 commit。

## 本地提交与推送

- 收到“确认提交”后，先检查全部状态和差异，排除敏感信息、临时文件、无关改动及用户未确认的内容。
- 按实际功能范围拆分为清晰、独立的 commits；数量不固定。
- 每个提交的摘要（Summary）和说明（Description）必须使用英文，并简短准确地说明实际改动。
- 只创建本地 commits，绝不执行 `git push`；用户通过 GitHub Desktop 手动推送。
- 未经用户明确要求，不修改已有提交（amend）、变基（rebase）、强制推送或执行其他可能改写历史的操作。
